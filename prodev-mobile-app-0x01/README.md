## Steps you followed for scaffolding.
The following steps were taken to scaffold the project;
1. `npx create-expo-app@latest`
2. choose y if prompted to continue
3. enter the name of the app
4. the folder will be created followed immediately by npm installations

## Observations from Running `npm run reset-project`
- `/app` moved to `/app-example/app`.
- `/components` moved to `/app-example/components`.
- `/hooks` moved to `/app-example/hooks`.
- `/constants` moved to `/app-example/constants`.
- `/scripts` moved to `/app-example/scripts`.
- New `/app` directory created.
- `app/index.tsx` created.
- `app/_layout.tsx` created.